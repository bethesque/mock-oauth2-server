package no.nav.security.mock.oauth2.logout

class LogoutManagement {
    val frontChannelLogout: FrontChannelLogout = FrontChannelLogout()
    val backChannelLogout: BackChannelLogout = BackChannelLogout()
}
